# VLM track dependencies (medical Vision-Language Model fine-tune + inference)
# Install:  uv pip install -e ".[vlm]"   (or pip install -r requirements-vlm.txt)
transformers>=4.45
peft>=0.13
bitsandbytes>=0.43
accelerate>=1.0
huggingface_hub>=0.25
datasets>=3.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
numpy>=1.24.0
pyyaml>=6.0
tqdm>=4.65.0
ollama>=0.1.0
