# Example dataset_info.yaml — D4 Chest X-ray
#
# Place this file as dataset_info.yaml inside your data directory
# alongside data_list.yaml and images/
#
# This file tells the pipeline to skip CT-specific preprocessing
# (no windowing, no mask intensity) and use resize + repeat only.

modality: X-ray
